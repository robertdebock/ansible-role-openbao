---
# Example group variables for an OpenBao HA cluster.
#
# These values are intended as a starting point. Adapt them to match
# your environment before running the example.

# Version of OpenBao to install/upgrade to on all nodes.
openbao_version: "2.5.1"

# Distribution-specific package naming for a versioned install.
# The format of a versioned package name differs per distribution:
_openbao_package_map:
  default: "openbao-{{ openbao_version }}"
  Debian: "openbao={{ openbao_version }}"
  Ubuntu: "openbao={{ openbao_version }}"
openbao_package_name: "{{ _openbao_package_map[ansible_facts['os_family']] | default(_openbao_package_map['default']) }}"

# Token used by the example upgrade playbook when calling the `bao`
# CLI (for example, to run `bao status`).
#
# IMPORTANT: Replace this placeholder with a real root/admin token
# before running the example.
openbao_admin_token: "changeme-root-token"

